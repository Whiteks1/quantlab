{
  "name": "quantlab-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "QuantLab Desktop shell for research, runtime visibility, and bounded AI assistance.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "smoke": "npm run smoke:fallback",
    "smoke:fallback": "node scripts/smoke.js --mode=fallback",
    "smoke:real-path": "node scripts/smoke.js --mode=real-path",
    "mcp": "node mcp-server.mjs"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.20.0",
    "electron": "^37.2.0",
    "zod": "^4.1.12"
  }
}
