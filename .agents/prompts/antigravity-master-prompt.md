Current task:
Issue #11 — Verify CLI modularization is complete.

Goal:
Confirm that main.py only parses arguments and routes commands, while command logic lives in src/quantlab/cli/.

Do not implement immediately.
First inspect the repository and report:
- what is already complete
- what remains incomplete
- the next logical step only

Current task:
Extract portfolio command handling from main.py into src/quantlab/cli/portfolio.py.

Constraints:
- preserve behavior
- do not modify business logic
- keep the change minimal
- verify tests after implementation

Do not implement yet.
First inspect and propose the next exact step.

Current task:
Review saved git stashes and determine whether they should be restored, discarded, or moved into new branches.

Do not apply anything yet.
First inspect and classify the stashes.