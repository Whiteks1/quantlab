# Artifact Contracts — QuantLab

## Purpose

This document defines the expected artifacts produced by QuantLab workflows.

Artifacts are the **source of truth** for experiment results and must remain stable across versions.

Artifacts must clearly communicate:

- what was executed
- which configuration was used
- what results were produced
- when the run occurred

Artifacts should be deterministic and reproducible.

---

# Run-Scoped Artifact Rule

All experiment artifacts should belong to a specific **run directory**.

Run directories live under:

outputs/runs/<run_id>/

A valid run directory must contain enough artifacts to reproduce the experiment context and results.

Artifacts written outside a run directory should be considered **temporary or legacy** unless explicitly documented.

---

# Run Directory Structure

A typical run directory should follow this structure:

outputs/
└─ runs/
   └─ <run_id>/
      ├ metadata.json
      ├ config.json
      ├ metrics.json
      ├ report.md
      ├ report.json
      ├ trades.csv
      └ artifacts/

The `artifacts/` folder can contain optional files such as:

- charts
- enriched datasets
- intermediate analytics

---

# Run Artifact Contracts

## outputs/runs/<run_id>/metadata.json

### Purpose

Stores identity and contextual metadata for a run.

### Generated by

Run system / execution layer.

### Minimum fields

run_id  
created_at  
mode  
strategy  
ticker  
status  

### Optional fields

stage  
tags  
notes  
source_command  
git_commit  

---

## outputs/runs/<run_id>/config.json

### Purpose

Stores the exact configuration used for the run.

This ensures experiments are reproducible.

### Generated by

Run system / execution layer.

### Minimum fields

ticker  
start_date  
end_date  
initial_capital  
strategy_params  

### Optional fields

fees_bps  
slippage_bps  
timeframe  
allocation_mode  
selection_rules  

---

## outputs/runs/<run_id>/metrics.json

### Purpose

Machine-readable summary metrics of a run.

Used for:

- run comparison
- ranking strategies
- automated analysis

### Generated by

Reporting / analytics layer.

### Minimum fields

total_return  
sharpe  
max_drawdown  
trades  
win_rate  

### Optional fields

profit_factor  
expectancy  
volatility  
avg_holding_period  
exposure  

---

## outputs/runs/<run_id>/report.md

### Purpose

Human-readable report describing the run.

Used for:

- manual analysis
- research documentation
- experiment review

### Typical sections

Run configuration  
Performance summary  
Trade statistics  
Risk metrics  
Charts (optional)

---

## outputs/runs/<run_id>/report.json

### Purpose

Machine-readable report used for automation and research tooling.

### Minimum fields

run_id  
strategy  
ticker  
total_return  
sharpe  
max_drawdown  
trades  

---

## outputs/runs/<run_id>/trades.csv

### Purpose

Raw trade log generated by backtest or paper execution.

### Generated by

Backtest or execution modules.

### Required columns

timestamp  
ticker  
action  
price  
size  
capital_before  
capital_after  

---

## outputs/runs/<run_id>/trades_enriched.csv

### Purpose

Trade-level analytics derived from raw trade logs.

### Generated by

Trade analytics workflows.

### Expected columns

entry_time  
exit_time  
holding_period  
gross_pnl  
net_pnl  
return_pct  
mae  
mfe  

---

# Shared Index Artifacts

## outputs/runs/registry.csv

### Purpose

Central index of all recorded runs.

Provides a lightweight registry for comparing and filtering experiments.

### Generated by

Run registry module.

### Minimum columns

run_id  
created_at  
mode  
strategy  
ticker  
start_date  
end_date  
status  

### Recommended columns

total_return  
sharpe  
max_drawdown  
trades  
win_rate  
tags  

---

# Legacy Artifact Contracts

Earlier workflows may still write artifacts directly under:

outputs/

Examples:

outputs/report.json  
outputs/report.md  
outputs/trades.csv  

These are considered **legacy outputs**.

The preferred long-term structure is **run-scoped artifacts under outputs/runs/**.

---

# Artifact Stability Rule

Artifact schemas must not change silently.

Changes to:

- field names
- required columns
- artifact paths
- schema meaning

must be accompanied by:

1. update to this document  
2. entry in `decision-log.md`  
3. session log note if the change affects active work  

Backward compatibility should be preserved whenever practical.

---

# Design Philosophy

Artifacts are treated as **first-class outputs** of the research process.

QuantLab prioritizes:

- reproducibility
- traceability
- experiment comparability
- deterministic reporting

Artifacts should allow any run to be understood and reproduced without relying on chat history or memory.